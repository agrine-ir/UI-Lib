namespace Agrine.UI.Sounds.Core
{
    public static class Enums
    {
        public enum StartupSoundTypes
        {
            SSoundOne, SSoundTwo, SSoundThree, SSoundFour, SSoundFive, SSoundSix
        }

        public enum ClickSoundTypes
        {
            CSoundOne, CSoundTwo
        }

        public enum AlertSoundTypes
        {
            ASoundOne, ASoundTwo
        }

        public enum MessageSoundTypes
        {
            MSoundOne, MSoundTwo, MSoundThree
        }
    }
}
